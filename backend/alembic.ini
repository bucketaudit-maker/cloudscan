# Alembic config for CloudScan (PostgreSQL)
# Run from backend/: alembic upgrade head

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

[post_write_hooks]
# hooks = black
